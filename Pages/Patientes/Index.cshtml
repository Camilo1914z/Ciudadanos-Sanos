@page
@model Ciudadanos_Sanos.Pages.Patientes.IndexModel
@{
    ViewData["Title"] = "Patientes";
}

<h1> Registrer Patientes</h1>
<p>

    <a asp-page="Create" class ="btn btn-success">Create New</a>

</p>
<table class="table" >
    <thead>

        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Patientes[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Patientes[0].Document)
            </th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Patientes)
        {
                    <tr>
                        <td>
                    @Html.DisplayFor(modelItem => item.Name)
                        </td>
                        <td>
                    @Html.DisplayFor(modelItem => item.Document)
                        </td>
                        <td>
                        <a asp-page="./Edit" asp-route-id="@item.Id" class="btn btn-warning">Edit</a>
                        <a asp-page="./Delete" asp-route-id="@item.Id" class="btn btn-danger">Delete</a>
                        </td>
                    </tr>
        }
    </tbody>
</table>
